# 20260224-100846 非直播关键功能对比检查

## 任务
按 f2 `v0.0.1.8-pw3` 对比当前项目，聚焦非直播关键功能；仅检查不改代码。

## 待办
- [x] 并行对比 handler 非直播能力映射
- [x] 并行对比 crawler/endpoint 非直播能力映射
- [x] 并行对比 downloader 非直播关键行为
- [x] 并行对比 CLI/导出 API 覆盖
- [x] 汇总“实现不同/未实现”清单

## 执行结果
- 已并行完成 4 组对照：`handler`、`crawler+endpoint`、`downloader`、`cli/api`。
- 非直播关键缺失能力：`user_active_status`、`user_short_info`、`user_feed_videos`、`post_danmaku`、`post_time_danmaku`。
- 关键行为差异：`fetchQueryUser` 请求语义与 f2 不一致；`fetchPostStats` body 语义与 f2 不一致；封面下载缺少 `dynamic_cover` 回退链路。
- CLI 覆盖差异：f2 的 like/collection/collects/mix 等模式未在当前 CLI 提供。
