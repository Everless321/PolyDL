# 20260224-102658 修复非直播实现差异（三项）

## 任务
修复与 f2 对比出的 3 个“实现不同”点：
1. `fetchQueryUser` 请求语义
2. `fetchPostStats` 提交语义
3. 封面下载缺失 `dynamic_cover` 回退链路

## 待办
- [x] 调整 `fetchQueryUser`：默认 GET，对旧参数保持兼容
- [x] 调整 `fetchPostStats`：按参数体参与签名并提交
- [x] 贯通 `dynamicCover` 字段并接入下载回退逻辑
- [x] 进行最小验证（typecheck）

## 执行结果
- `fetchQueryUser` 现在在无参数时按 f2 语义走 GET；传参时仍兼容原 POST 方式。
- `fetchPostStats` 改为使用参数体字符串参与签名并提交 body。
- 增加 `dynamicCover` 字段：`filter -> AwemeData -> downloader` 全链路打通，下载封面回退变为 `animated -> dynamic -> static`。
- `npm run typecheck` 通过。
