# 20260224-095318 项目用途分析

## 任务
分析项目是做什么的，梳理核心能力与模块分工（不执行测试命令）。

## 待办
- [x] 识别项目定位与主要使用场景
- [x] 梳理核心模块与调用链路
- [x] 总结当前实现边界与风险点

## 执行结果
- 已确认该项目是一个 `Node.js + TypeScript` 的抖音内容抓取与下载工具库，包含 CLI 与 SDK 两种使用方式。
- 已确认核心能力包括：单作品下载、用户主页批量下载、封面/音乐/文案下载、直播信息与部分直播流处理。
- 已确认主链路为：`CLI/SDK -> Handler -> Crawler -> Filter -> Downloader`，并通过 `X-Bogus/A-Bogus` 签名与 `msToken` 机制访问网页端接口。
- 已识别边界：部分导出 API 仍是占位实现，直播流下载当前主要给出 ffmpeg 提示命令。
