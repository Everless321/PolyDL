# 20260224-103148 重新应用三项差异修复

## 任务
在用户还原后，重新应用三项修复：
1. `fetchQueryUser` 语义（默认 GET，兼容旧 POST）
2. `fetchPostStats` 提交语义（body 参与签名并提交）
3. 封面下载增加 `dynamic_cover` 回退链路

## 待办
- [x] 修复 crawler 的 `fetchQueryUser`
- [x] 修复 crawler 的 `fetchPostStats`
- [x] 调整 handler `fetchQueryUser` 参数为可选
- [x] 打通 `dynamicCover`（filter -> types -> downloader）
- [x] 最小验证 typecheck

## 执行结果
- `fetchQueryUser` 已支持无参默认 GET（对齐 f2），保留传参时 POST 兼容逻辑。
- `fetchPostStats` 已改为参数体字符串参与签名并作为 body 提交。
- `dynamicCover` 已加入 `AwemeData`，并在 `UserPostFilter` / `PostDetailFilter` 中提取后传递到下载器。
- 封面下载回退链路更新为 `animated -> dynamic -> static`。
- `npm run typecheck` 通过。
